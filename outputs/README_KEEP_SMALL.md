# Outputs Policy

`outputs/` is reserved for small reports and lightweight summaries only.

Large Abaqus products, model outputs, training checkpoints, generated candidate batches, and temporary solver files must stay outside Git. Stage 3 evidence should be summarized into versioned docs and compact manifests under `docs/` and `artifacts/manifests/`.
